# dtb_parser — freestanding FDT (v17) walker. Header-only pure model:
# the runtime guest_table construction and any later DTB consumer
# include it; nothing registers cib services.
nova_add_component(dtb_parser)
