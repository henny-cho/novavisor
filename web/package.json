{
  "name": "novavisor-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Toolchain for the workbench UI: the structural contracts a browser-only, build-step-free page cannot check for itself, the behaviour of its modules run outside a browser, and what it costs inside one.",
  "scripts": {
    "lint": "eslint .",
    "test": "npm run lint && node --test 'test/*.test.mjs'",
    "perf": "node perf/measure.mjs"
  },
  "devDependencies": {
    "eslint": "10.8.1",
    "playwright": "1.62.1"
  }
}
